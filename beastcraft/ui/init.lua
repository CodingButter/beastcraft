return {
    renderDom = require(BEASTCRAFT_ROOT .. "ui.domrenderer"),
    div = require(BEASTCRAFT_ROOT .. "dom").div,
    button = require(BEASTCRAFT_ROOT .. "dom").button,
    input = require(BEASTCRAFT_ROOT .. "dom").input,
    ul = require(BEASTCRAFT_ROOT .. "dom").ul
}
